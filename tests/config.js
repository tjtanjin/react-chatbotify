const config = {
    TEST_URL: "http://localhost:3000/", // usually unchanged, unless you use a different port
    WAIT_DURATION: 2000, // increase this if you're testing on a slow/laggy device
    HEADLESS: true, // disabling this may help with visually debugging
}

export default config;